export enum NotificationStatus {
    QUEUED = 'queued',
    SCHEDULED = 'scheduled',
    SENDING = 'sending',
    SENT = 'sent',
    DELIVERED = 'delivered',
    FAILED = 'failed',
    READ = 'read',
}
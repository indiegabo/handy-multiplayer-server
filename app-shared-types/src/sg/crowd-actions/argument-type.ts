/**
 * Enumerates all supported argument types for chat commands and actions.
 *
 * @enum {number}
 */
export enum ArgumentType {
    None = 'none',
    String = 'string',
    Integer = 'integer',
    Float = 'float',
    Boolean = 'boolean',
}
export enum GameType {
    /**
     * Games that run while the streamer can focus elsewhere and
     * the community interacts without requiring direct attention.
     */
    Backseat = 1,

    /**
     * Games where the streamer actively plays together with the community.
     */
    Spotlight = 2,
}